#Workflow Name
name: ApplyGrid CI

#Events
on:
  push:
    branches:
      - main
#Jobs
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Show repository
        run: ls